import 'package:flutter/material.dart';

class CategoryCourses extends StatelessWidget {
  const CategoryCourses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Courses'),
      ),
    );
  }
}
